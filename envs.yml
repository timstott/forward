common: &common
  GIT_SHA: ${file(version.js):version}

dev:
  <<: *common
  NODE_ENV: dev
  LOG_LEVEL: silly
  SOURCE_ADDRESS: noreply@dev.forward.predicate.xyz

production:
  <<: *common
  NODE_ENV: production
  LOG_LEVEL: warn
  SOURCE_ADDRESS: noreply@forward.predicate.xyz
